# def create_file_with_size(file_path, size_in_mb):
#     size_in_bytes = size_in_mb * 1024 * 1024  # Chuyển đổi MB sang bytes
#     with open(file_path, 'wb') as f:
#         # Ghi dữ liệu ngẫu nhiên cho đến khi đạt kích thước mong muốn
#         f.write(b'\0' * size_in_bytes  )  # Ghi 1MB dữ liệu 0

# # Đường dẫn tới file mà bạn muốn tạo
# file_path = 'file_100MB.bin'
# create_file_with_size(file_path, 100)

# print(f"File '{file_path}' đã được tạo với kích thước 100MB.")

n = 10
print("thidvcbjbshv :",n)