/*               Hàm tạo của lớp
Hàm tạo đung để khởi gán giá trị cho các thuộc tính của đối tượng khi chúng đc sinh ra
 Cú pháp:
 -> Hàm tạo k đối
  Tên lớp() { lệnh gắn giá trị; }
  -> hàm tạo có đối
  Tên lớp(danh-sách-đối) { Lệnh gắn giá trị;}
  Hàm taoh của lớp bao( lớp mà thuộc tính chứa đối tượng của lớp khác) 
  chẳng hạn như lớp hình tròn có thuốc tính tam là đối tượng của lớp diem
  Hinhtron():tam(){r=1}
  Hinhtron(float a, float b, float bl):tam(a,b){r=bk;}
  Tenlopbao(ds_doi):doituong(ds_Gia_tri){lệnh giá trị cho tt thông thường)
Vi du class giay
Giay(){loai =1; xx="VN"; gia =100; ma=""}
Giay(string ma, string xx, float gia, int loai)
{ this->ma==ma;
  this->xx=xx;
  (*this).gia=gia;
  (*this).loai=loai;
}